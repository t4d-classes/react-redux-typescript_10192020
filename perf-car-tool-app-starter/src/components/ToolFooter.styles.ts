import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  footer: {
    textAlign: 'center',
    paddingTop: 6,
    paddingBottom: 12,
    backgroundColor: 'navy',
    color: 'white',
  },
  text: {
    fontSize: 12,
  }
});