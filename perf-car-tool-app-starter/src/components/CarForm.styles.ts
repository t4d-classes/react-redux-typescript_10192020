import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  form: {
    marginTop: 20,
  },
  formRow: {
    padding: '8px',
  },
  formControl: {
    width: 250,
  },
});
